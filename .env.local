# Local XAMPP MySQL Development Configuration
# This file configures the app for local development using XAMPP MySQL
MYSQL_DATABASE_URL=mysql://root:@localhost:3306/agile_tracker
DATABASE_URL=mysql://root:@localhost:3306/agile_tracker
NODE_ENV=development
PORT=5000

# Session secret for local development
SESSION_SECRET=local-dev-secret-key-123456789

# Local XAMPP MySQL connection details
DB_HOST=localhost
DB_PORT=3306  
DB_NAME=agile_tracker
DB_USER=root
DB_PASS=

# Note: Default XAMPP MySQL uses root user with no password
# Change these if your XAMPP MySQL has different credentials